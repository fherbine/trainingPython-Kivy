<MenuScreen>:
    BoxLayout:
        orientation: 'vertical'
        spacing: dp(10)
        pos_hint: {'center': (.5, .5)}
        size_hint: .5, .5

        Button:
            text: 'distance/calculation mode'

        Button:
            text: 'simulation mode'
